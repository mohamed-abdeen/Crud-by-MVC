@model IEnumerable<EmployeeViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>All Employees</h1>
<br />
<br />
<a asp-action="Create" class="btn btn-primary">Create New Employee </a>
<br />
<form asp-action="Index" class="my-3">
    <div class="row align-items-center justify-content-center">
        <div class="col-8">
            <input type="text" class="form-control" name="SearchValue" />

        </div>
        <div class="col-4">
            <input type="submit" value="search" class="btn btn-success " />
        </div>
    </div>
</form>

@if (Model.Count() > 0)
{
    <table class="mt-3 tabble table-striped table-hover ">
        <thead>
            <tr>

                <th>@Html.DisplayNameFor(D=>D.Name)</th>
                <th>@Html.DisplayNameFor(D=>D.Age)</th>
                <th>@Html.DisplayNameFor(D=>D.Adress)</th>
                <th>@Html.DisplayNameFor(D=>D.Image)</th>
                <th>@Html.DisplayNameFor(D=>D.Department)</th>
                <th>@Html.DisplayNameFor(D=>D.Salary)</th>
                <th>@Html.DisplayNameFor(D=>D.IsActive)</th>
                <th>@Html.DisplayNameFor(D=>D.Email)</th>
                <th>@Html.DisplayNameFor(D=>D.PhoneNumber)</th>
                <th>@Html.DisplayNameFor(D=>D.HireDate)</th>



                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var Employee in Model)
            {
                <tr>
                    >

                    <td>@Html.DisplayFor(m=>Employee.Name)</td>
                    <td>@Html.DisplayFor(m=>Employee.Age)</td>
                    <td>@Html.DisplayFor(m=>Employee.Adress)</td>
                    <td>
                        <img src="~/Files/Images/@Employee.ImageName" alt="Employee Image" class="img-fluid "height="20"/>
                    </td>
                    @*<td>@Html.DisplayFor(m=>Employee.Department.Name)</td>*@

                    @*<td>@Employee.IsActive</td>*@
                    <td>@Employee.IsActive</td>
                    <td>@Html.DisplayFor(m=>Employee.IsActive)</td>
                    <td>@Html.DisplayFor(m=>Employee.Email)</td>
                    <td>@Html.DisplayFor(m=>Employee.PhoneNumber)</td>
                    <td>@Html.DisplayFor(m=>Employee.HireDate)</td>
                    <partial name="_ButtonsPartialView" model="@Employee.Id.ToString();" />



                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="mt-5 alert-warning">
        <h3>There Is No Employee</h3>
    </div>
}